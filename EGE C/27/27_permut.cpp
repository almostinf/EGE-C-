#include <iostream>
#include <cmath>
#include <fstream>
#include <algorithm>
#include <vector>
using namespace std;


// vector<int> permut(int a, int b, int c) {
//     for (int )
// }

int main() {
    ifstream file("27_permut_A.txt");
    int size;
    vector<int> vec;
    vector<int> vec_out;
    file >> size;
    int a, b, c;
    file >> a >> b >> c;
    
    
    for (auto x : vec) cout << x << ' ';
    // while (!file.eof()) {

    // }
}